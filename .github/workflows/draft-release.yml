name: Build Draft Release

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-packages:
    name: Build packages
    uses: ./.github/workflows/build.yml

  draft-release:
    name: Create draft release
    needs: build-packages
    runs-on: ubuntu-latest
    steps:
      - name: Download release assets
        uses: actions/download-artifact@v8
        with:
          pattern: Fleasion-v${{ needs.build-packages.outputs.artifact_version }}-*
          path: release-files
          merge-multiple: true
          skip-decompress: true

      - name: Create draft GitHub release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: v${{ needs.build-packages.outputs.app_version }}
          target_commitish: ${{ github.sha }}
          name: Fleasion v${{ needs.build-packages.outputs.app_version }}
          draft: true
          prerelease: ${{ needs.build-packages.outputs.is_prerelease == 'true' }}
          generate_release_notes: true
          files: release-files/*
          fail_on_unmatched_files: true
